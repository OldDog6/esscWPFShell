<UserControl x:Class="esscWPFShell.Views.StatusBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="23" d:DesignWidth="1050">
    <Grid>
<StatusBar Margin="0" Name="appstatusBar" Grid.Row="1" Background="#FF041952" Foreground="LightGray" FontSize="10">
            <StatusBarItem Width="75" Content="{Binding AppStatusBar.UserName}"/>
            <StatusBarItem Width="50" Content="{Binding AppStatusBar.ApplicationMode}"/>
            <StatusBarItem Width="50" Content="{Binding AppStatusBar.DisplayModified}"/>
            <StatusBarItem Width="850" Content="{Binding AppStatusBar.DisplayMessage}"/>
        </StatusBar>
    </Grid>
</UserControl>
